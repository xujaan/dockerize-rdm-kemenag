version: "3"

services:
    php-server:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "8088:80"
        volumes:
            - ./:/var/www/html
        restart: always
